# Z-Index Layers Documentation

## Система слоёв приложения

Документация описывает иерархию z-index для всех UI компонентов проекта.

### Таблица уровней

| Уровень | Z-Index | Компоненты | Назначение |
|---------|---------|------------|------------|
| 🔴 Критичный | `z-100` | Toast, Toaster | Системные уведомления (успех, ошибка, предупреждение) |
| 🟠 Модальный | `z-90` | Dialog, Sheet, AlertDialog | Модальные окна и боковые панели |
| 🟡 Выпадающий | `z-80` | Dropdown, Popover, Select, Context Menu | Выпадающие меню и всплывающие элементы |
| 🟢 Тур | `z-70` | Tour tooltips | Подсказки интерактивного тура |
| 🔵 Уведомления | `z-60` | InstallPrompt, WelcomeNotification | Уведомления приложения (PWA, приветствие) |
| ⚪ Навигация | `z-50` | Header, Navigation | Фиксированная навигация и хедер |
| ⚫ Базовый | `z-0 - z-40` | - | Резерв для контентных элементов |

---

## Правила использования

### 1. Никогда не используйте произвольные z-index
❌ **Неправильно:**
```tsx
<div className="z-[999]">...</div>
<div style={{ zIndex: 9999 }}>...</div>
```

✅ **Правильно:**
```tsx
<div className="z-[90]">...</div> // Для модальных окон
<div className="z-[80]">...</div> // Для выпадающих меню
```

### 2. Используйте уровни из таблицы
Всегда выбирайте z-index из установленных уровней в зависимости от типа компонента.

### 3. Новые компоненты
При создании нового UI компонента определите его категорию:
- **Системное уведомление?** → `z-100`
- **Модальное окно?** → `z-90`
- **Выпадающее меню?** → `z-80`
- **Тур-подсказка?** → `z-70`
- **Уведомление приложения?** → `z-60`
- **Навигация?** → `z-50`

---

## Компоненты по категориям

### Z-100: Системные уведомления
**Файлы:**
- `src/components/ui/toast.tsx`
- `src/components/ui/toaster.tsx`

**Описание:** Должны быть всегда поверх всех элементов, так как критичны для UX.

---

### Z-90: Модальные окна
**Файлы:**
- `src/components/ui/dialog.tsx` — базовое модальное окно
- `src/components/ui/alert-dialog.tsx` — диалоги подтверждения
- `src/components/ui/sheet.tsx` — боковые панели

**Описание:** Блокируют взаимодействие с остальным контентом. Overlay (затемнение) и Content используют один z-index.

**Примеры использования:**
- Формы редактирования
- Подтверждения действий
- Боковые меню настроек

---

### Z-80: Выпадающие элементы
**Файлы:**
- `src/components/ui/dropdown-menu.tsx` — выпадающие меню
- `src/components/ui/popover.tsx` — всплывающие подсказки
- `src/components/ui/select.tsx` — селекты
- `src/components/ui/context-menu.tsx` — контекстные меню

**Описание:** Появляются при клике/ховере, не блокируют весь интерфейс. Должны быть ниже модальных окон, но выше навигации.

**Примеры использования:**
- Меню действий
- Выбор опций
- Контекстные действия

---

### Z-70: Интерактивный тур
**Файлы:**
- `src/components/Tour.tsx`

**Описание:** Подсказки для новых пользователей. Должны быть ниже модалок, но выше уведомлений.

---

### Z-60: Уведомления приложения
**Файлы:**
- `src/components/InstallPrompt.tsx` — уведомление о PWA
- `src/components/WelcomeNotification.tsx` — приветственное уведомление

**Описание:** Информационные уведомления, которые можно закрыть. Ниже всех интерактивных элементов.

---

### Z-50: Навигация
**Файлы:**
- `src/components/Header.tsx` — главный хедер

**Описание:** Фиксированная навигация, которая всегда видна при прокрутке.

---

## Отладка конфликтов z-index

### Проблема: Элемент перекрывает модальное окно
1. Проверьте z-index элемента — должен быть `< z-90`
2. Убедитесь, что родительский контейнер не создаёт новый stacking context

### Проблема: Выпадающее меню под хедером
1. Проверьте, что dropdown использует `z-80` (выше, чем `z-50` у хедера)
2. Убедитесь, что dropdown рендерится в Portal (вне родительского контейнера)

### Проблема: Toast не видно
1. Toast должен использовать `z-100` — выше всех элементов
2. Проверьте, что Toaster компонент правильно подключён

---

## Дополнительные ресурсы

- [MDN: z-index](https://developer.mozilla.org/en-US/docs/Web/CSS/z-index)
- [MDN: Stacking context](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_positioned_layout/Understanding_z-index/Stacking_context)
- [Tailwind CSS: Z-Index](https://tailwindcss.com/docs/z-index)

---

**Последнее обновление:** 2025-12-14  
**Автор:** Юра (poehali.dev)
